

const sum = require('../app.js');

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

test('A', ()=>{
  if(this.name === "Ann" && this.age === 50,
  expect(person).toBeTruthy()
);
}
)

test('return items that have 6 characters', () => {
  expect(new Set(favFood)).toContain('noodles', 'tortillas');
});

test('word', ()=> {
  if(string === "ten")
  expect(word).toBeTruthy

})

test ("plan", () =>{
  if(planets(2) === "Earth")
  expect(plan).toBeTruthy
})









